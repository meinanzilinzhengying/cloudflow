apiVersion: v1
kind: Secret
metadata:
  name: cloudflow-agent-secrets
  namespace: cloudflow
  labels:
    app: cloudflow-agent
type: Opaque
stringData:
  # 建议使用：echo -n "your-api-key" | base64
  # 或者使用 kubectl create secret
  api-key: ""
